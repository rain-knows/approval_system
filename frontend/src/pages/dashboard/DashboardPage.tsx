/**
 * 仪表盘页面组件
 *
 * 数据看板页面（留空占位）。
 */

import { PageContainer } from '@/components/layout/PageContainer'

/**
 * 仪表盘页面
 *
 * 返回：仪表盘页面组件
 */
export default function DashboardPage() {
    return (
        <PageContainer title="数据看板" description="该页面暂留空白，待后续完善。">
            <div className="min-h-60" />
        </PageContainer>
    )
}
